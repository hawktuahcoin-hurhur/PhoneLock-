# Netlify: connect this repo and it deploys docs/ (the web app) as-is (no build step).
[build]
  publish = "docs"
  command = ""

[[headers]]
  for = "/sw.js"
  [headers.values]
    Cache-Control = "no-cache"
